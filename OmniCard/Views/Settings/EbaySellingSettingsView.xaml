<UserControl x:Class="OmniCard.Views.Settings.EbaySellingSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:OmniCard.Views.Settings"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type local:EbaySellingSettingsViewModel}}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16" MaxWidth="560" HorizontalAlignment="Left">

            <TextBlock Text="eBay Selling" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,12" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>

            <TextBlock Text="Ship-from address" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Location name" VerticalAlignment="Center" Margin="0,4" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBox   Grid.Row="0" Grid.Column="1" Margin="0,4" Text="{Binding Settings.LocationName, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Address line 1" VerticalAlignment="Center" Margin="0,4" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBox   Grid.Row="1" Grid.Column="1" Margin="0,4" Text="{Binding Settings.AddressLine1, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Address line 2" VerticalAlignment="Center" Margin="0,4" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBox   Grid.Row="2" Grid.Column="1" Margin="0,4" Text="{Binding Settings.AddressLine2, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBlock Grid.Row="3" Grid.Column="0" Text="City" VerticalAlignment="Center" Margin="0,4" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBox   Grid.Row="3" Grid.Column="1" Margin="0,4" Text="{Binding Settings.City, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBlock Grid.Row="4" Grid.Column="0" Text="State" VerticalAlignment="Center" Margin="0,4" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBox   Grid.Row="4" Grid.Column="1" Margin="0,4" Text="{Binding Settings.State, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Postal code" VerticalAlignment="Center" Margin="0,4" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBox   Grid.Row="5" Grid.Column="1" Margin="0,4" Text="{Binding Settings.PostalCode, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBlock Grid.Row="6" Grid.Column="0" Text="Country (ISO-2)" VerticalAlignment="Center" Margin="0,4" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBox   Grid.Row="6" Grid.Column="1" Margin="0,4" Text="{Binding Settings.Country, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBlock Grid.Row="7" Grid.Column="0" Text="Phone" VerticalAlignment="Center" Margin="0,4" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBox   Grid.Row="7" Grid.Column="1" Margin="0,4" Text="{Binding Settings.Phone, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
            </Grid>

            <TextBlock Text="Shipping" FontWeight="SemiBold" FontSize="14" Margin="0,16,0,12" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
            <CheckBox Content="Free shipping" Margin="0,4" IsChecked="{Binding FreeShipping}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Shipping cost" VerticalAlignment="Center" Margin="0,4" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBox   Grid.Row="0" Grid.Column="1" Width="80" HorizontalAlignment="Left" Margin="0,4"
                           IsEnabled="{Binding NotFreeShipping}"
                           Text="{Binding Settings.ShippingCost, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Handling time (days)" VerticalAlignment="Center" Margin="0,4" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBox   Grid.Row="1" Grid.Column="1" Width="80" HorizontalAlignment="Left" Margin="0,4"
                           Text="{Binding Settings.HandlingTimeDays, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
            </Grid>

            <TextBlock Text="Returns" FontWeight="SemiBold" FontSize="14" Margin="0,16,0,12" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
            <CheckBox Content="Accept returns" Margin="0,4" IsChecked="{Binding Settings.ReturnsAccepted}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Return window (days)" VerticalAlignment="Center" Margin="0,4" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBox   Grid.Row="0" Grid.Column="1" Width="80" HorizontalAlignment="Left" Margin="0,4"
                           Text="{Binding Settings.ReturnWindowDays, UpdateSourceTrigger=PropertyChanged}" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Return shipping paid by" VerticalAlignment="Center" Margin="0,4" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                <ComboBox  Grid.Row="1" Grid.Column="1" Width="120" HorizontalAlignment="Left" Margin="0,4"
                           ItemsSource="{Binding ReturnShippingPayerValues}"
                           SelectedItem="{Binding Settings.ReturnShippingPaidBy}"/>
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                <Button Content="Save" Command="{Binding SaveCommand}" IsEnabled="{Binding NotBusy}" Padding="16,4" Margin="0,0,12,0"/>
                <Button Content="Run eBay Setup" Command="{Binding RunSetupCommand}" IsEnabled="{Binding NotBusy}" Padding="16,4"/>
            </StackPanel>

            <TextBlock Text="Setup status" FontWeight="SemiBold" FontSize="14" Margin="0,16,0,8" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
            <ItemsControl ItemsSource="{Binding StatusLog}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="0,2" TextWrapping="Wrap" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
