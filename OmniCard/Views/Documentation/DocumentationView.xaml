<Window x:Class="OmniCard.Views.Documentation.DocumentationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Title="OmniCard Help &amp; Documentation" Height="760" Width="1080"
        MinHeight="480" MinWidth="720"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{StaticResource AppFont}">

    <Grid>
        <wv2:WebView2 x:Name="WebView"/>

        <!-- Shown when the docs bundle is missing from the build output. -->
        <TextBlock x:Name="MissingDocsMessage"
                   Visibility="Collapsed"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   TextAlignment="Center" TextWrapping="Wrap"
                   MaxWidth="420"
                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                   Text="Documentation content could not be found. Please reinstall or rebuild the application to restore the bundled help files."/>
    </Grid>
</Window>
