<Window x:Class="OmniCard.Views.Sales.RequireReasonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OmniCard.Views.Sales"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Title="{Binding ViewModel.Title}" Height="260" Width="420"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{StaticResource AppFont}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding ViewModel.Message}" TextWrapping="Wrap" Margin="0,0,0,8"
                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>

        <TextBox Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                 Text="{Binding ViewModel.Reason, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Command="{Binding ViewModel.CancelCommand}"
                    IsCancel="True" Padding="16,6" Margin="0,0,8,0"/>
            <Button Content="Confirm" Command="{Binding ViewModel.ConfirmCommand}"
                    IsDefault="True" IsEnabled="{Binding ViewModel.CanConfirm}" Padding="16,6" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
