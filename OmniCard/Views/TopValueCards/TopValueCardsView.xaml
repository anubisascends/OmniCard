<Window x:Class="OmniCard.Views.TopValueCards.TopValueCardsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OmniCard.Views.TopValueCards"
        xmlns:conv="clr-namespace:OmniCard.Controls.Converters;assembly=OmniCard.Controls"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Title="Top 100 Cards" Height="640" Width="920"
        MinHeight="400" MinWidth="700"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{StaticResource AppFont}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Your highest market-value cards that are not currently listed for sale, across every game."
                   Margin="0,0,0,8"
                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"/>

        <ProgressBar Grid.Row="0"
                     Height="4"
                     Margin="0,28,0,0"
                     VerticalAlignment="Bottom"
                     IsIndeterminate="True"
                     Visibility="{Binding ViewModel.IsBusy, Converter={conv:BoolToVisibilityConverter}}"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding ViewModel.Cards}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Width="2*">
                    <DataGridTextColumn.Binding>
                        <MultiBinding StringFormat="{}{0}{1}">
                            <Binding Path="Name"/>
                            <Binding Path="IsFoil" Converter="{conv:FoilSuffixConverter}"/>
                        </MultiBinding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Game" Binding="{Binding Game}" Width="90"/>
                <DataGridTextColumn Header="Set" Binding="{Binding SetCode}" Width="70"/>
                <DataGridTextColumn Header="#" Binding="{Binding Number}" Width="60"/>
                <DataGridTextColumn Header="Condition" Binding="{Binding Condition}" Width="80"/>
                <DataGridTextColumn Header="Market Price" Binding="{Binding MarketPrice, StringFormat='{}{0:C2}'}" Width="100"/>
                <DataGridTextColumn Header="Location" Binding="{Binding Container.Name, TargetNullValue='Unassigned'}" Width="1*"/>
                <DataGridTemplateColumn Header="" Width="130">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Go to Location"
                                    Padding="8,2"
                                    Command="{Binding DataContext.ViewModel.GoToLocationCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Empty state -->
        <TextBlock Grid.Row="1"
                   Text="No unlisted cards found."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="16"
                   FontStyle="Italic"
                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"
                   IsHitTestVisible="False">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ViewModel.Cards.Count}" Value="0"/>
                                <Condition Binding="{Binding ViewModel.IsBusy}" Value="False"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <Grid Grid.Row="2" Margin="0,8,0,0">
            <TextBlock Text="{Binding ViewModel.Cards.Count, StringFormat='{}{0} card(s)'}"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"/>
            <Button Content="Close"
                    IsCancel="True"
                    Padding="16,6"
                    FontWeight="SemiBold"
                    HorizontalAlignment="Right"
                    Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</Window>
