<UserControl x:Class="OmniCard.Views.Root.SealedProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:OmniCard.Views.Root">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <!-- Entry toolbar -->
        <Border Padding="8" Background="{DynamicResource MaterialDesign.Brush.Card.Background}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="UPC / Barcode:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Text="{Binding UpcEntry, UpdateSourceTrigger=PropertyChanged}"
                         Width="200" VerticalContentAlignment="Center" Margin="0,0,4,0">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding AddByUpcCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <Button Content="Add" Command="{Binding AddByUpcCommand}"
                        Padding="8,4" Margin="0,0,12,0"/>
                <Separator/>
                <Button Content="New Product..." Command="{Binding AddByTemplateCommand}"
                        Padding="8,4" Margin="8,0,0,0"/>
                <Separator/>
                <Button Content="Manage Templates..." Command="{Binding ManageTemplatesCommand}"
                        Padding="8,4" Margin="8,0,0,0"
                        Style="{StaticResource MaterialDesignFlatButton}"/>
            </StackPanel>
        </Border>

        <!-- Product list -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Instances}"
                  SelectedItem="{Binding SelectedInstance}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Template.Name}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Template.ProductType}" Width="120"/>
                <DataGridTextColumn Header="Set" Binding="{Binding Template.SetCode}" Width="60"/>
                <DataGridTextColumn Header="UPC" Binding="{Binding Template.Upc}" Width="120"/>
                <DataGridTextColumn Header="Purchase $"
                                    Binding="{Binding PurchasePrice, StringFormat={}{0:F2}, TargetNullValue=''}"
                                    Width="80"/>
                <DataGridTextColumn Header="Date Added"
                                    Binding="{Binding DateAdded, StringFormat={}{0:yyyy-MM-dd}}"
                                    Width="100"/>
            </DataGrid.Columns>

            <DataGrid.ContextMenu>
                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                    <MenuItem Header="Crack Open" Command="{Binding CrackInstanceCommand}"/>
                    <MenuItem Header="Edit Template..." Command="{Binding EditInstanceCommand}"/>
                    <Separator/>
                    <MenuItem Header="Delete" Command="{Binding DeleteInstanceCommand}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

        <!-- Empty state -->
        <TextBlock Grid.Row="1"
                   Text="No Sealed Products"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="18"
                   FontStyle="Italic"
                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"
                   IsHitTestVisible="False">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Instances.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
