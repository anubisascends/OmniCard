<UserControl x:Class="OmniCard.Views.Root.SealedProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:OmniCard.Views.Root"
             xmlns:editor="clr-namespace:OmniCard.Views.SealedProductEditor">

    <UserControl.Resources>
        <editor:ProductTypeDisplayConverter x:Key="ProductTypeDisplayConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <!-- Entry toolbar -->
        <Border Padding="8" Background="{DynamicResource MaterialDesign.Brush.Card.Background}">
            <StackPanel Orientation="Horizontal">
                <Button Content="Add Products..." Command="{Binding AddProductsCommand}"
                        Padding="12,4" FontWeight="SemiBold" Margin="0,0,12,0"/>
                <Separator/>
                <Button Content="Manage Templates..." Command="{Binding ManageTemplatesCommand}"
                        Padding="8,4" Margin="8,0,0,0"
                        Style="{StaticResource MaterialDesignFlatButton}"/>
            </StackPanel>
        </Border>

        <!-- Product list -->
        <DataGrid Grid.Row="1"
                  x:Name="SealedProductGrid"
                  ItemsSource="{Binding Instances}"
                  SelectedItem="{Binding SelectedInstance}"
                  AutoGenerateColumns="False"
                  IsReadOnly="False"
                  SelectionMode="Single"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  CellEditEnding="SealedProductGrid_CellEditEnding">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Template.Name}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Type"
                                    Binding="{Binding Template.ProductType, Converter={StaticResource ProductTypeDisplayConverter}}"
                                    Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Set" Binding="{Binding Template.SetCode}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="UPC" Binding="{Binding Template.Upc}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Purchase $"
                                    Binding="{Binding PurchasePrice, StringFormat={}{0:F2}, TargetNullValue=''}"
                                    Width="80"/>
                <DataGridTextColumn Header="Date Added"
                                    Binding="{Binding DateAdded, StringFormat={}{0:yyyy-MM-dd}}"
                                    Width="100" IsReadOnly="True"/>
            </DataGrid.Columns>

            <DataGrid.ContextMenu>
                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                    <MenuItem Header="Crack Open" Command="{Binding CrackInstanceCommand}"/>
                    <MenuItem Header="Edit Template..." Command="{Binding EditInstanceCommand}"/>
                    <Separator/>
                    <MenuItem Header="Delete" Command="{Binding DeleteInstanceCommand}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

        <!-- Empty state -->
        <TextBlock Grid.Row="1"
                   Text="No Sealed Products"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="18"
                   FontStyle="Italic"
                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"
                   IsHitTestVisible="False">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Instances.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
