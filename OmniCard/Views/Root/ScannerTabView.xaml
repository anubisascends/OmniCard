<UserControl x:Class="OmniCard.Views.Root.ScannerTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:OmniCard.Views.Root"
             xmlns:helpers="clr-namespace:OmniCard.Helpers"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:Models="clr-namespace:OmniCard.Models">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <!-- Audit mode banner -->
        <Border Grid.Row="0"
                Background="#FF1565C0"
                Padding="12,8"
                Visibility="{Binding ViewModel.IsAuditMode, Converter={StaticResource BoolToVis}}">
            <DockPanel>
                <Button DockPanel.Dock="Right"
                        Content="Cancel Audit"
                        Command="{Binding ViewModel.EndAuditCommand}"
                        Margin="12,0,0,0"
                        Padding="12,4"/>
                <TextBlock Foreground="White" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center">
                    <Run Text="Auditing: "/>
                    <Run Text="{Binding ViewModel.AuditLocationName, Mode=OneWay}"/>
                </TextBlock>
            </DockPanel>
        </Border>

        <ToolBarPanel Grid.Row="1">
            <ToolBarTray>
                <ToolBar>
                    <TextBlock Text="Only match from set(s):"
                               VerticalAlignment="Center"
                               Margin="8,0,4,0"/>
                    <TextBox x:Name="SetFilterTextBox"
                             Text="{Binding ViewModel.SetFilterText, UpdateSourceTrigger=PropertyChanged}"
                             MinWidth="200"
                             VerticalAlignment="Center"
                             VerticalContentAlignment="Center"
                             Margin="0,0,4,0"
                             Tag="All Sets"
                             ToolTip="Comma-separated set codes (e.g. vow, dmu, tla). Press Enter to apply.">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return"
                                        Command="{Binding ViewModel.ApplySetFilterTextCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button Content="..."
                            ToolTip="Open Set Filter Builder"
                            Padding="8,2"
                            VerticalAlignment="Center"
                            Margin="0,0,4,0"
                            Command="{Binding ViewModel.OpenSetFilterBuilderCommand}"/>
                    <Button Content="X"
                            ToolTip="Clear set filter"
                            FontSize="10"
                            Padding="4,2"
                            VerticalAlignment="Center"
                            Margin="0,0,8,0"
                            Command="{Binding ViewModel.ClearSetFilterCommand}"
                            Style="{StaticResource MaterialDesignFlatButton}"/>
                    <Separator Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}"/>
                    <TextBlock Text="Location:"
                               VerticalAlignment="Center"
                               Margin="0,0,4,0"
                               Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}"/>
                    <ComboBox ItemsSource="{Binding ViewModel.GroupedContainers}"
                              SelectedItem="{Binding ViewModel.ActiveContainer}"
                              DisplayMemberPath="Name"
                              Width="150"
                              VerticalAlignment="Center"
                              Margin="0,0,4,0"
                              Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}">
                        <ComboBox.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"
                                                   Margin="4,4,0,2"/>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ComboBox.GroupStyle>
                    </ComboBox>

                    <!-- Binder position fields (hidden in audit mode) -->
                    <StackPanel Orientation="Horizontal"
                                Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}">
                        <TextBlock Text="Page:"
                                   VerticalAlignment="Center"
                                   Margin="4,0,4,0"
                                   Visibility="{Binding ViewModel.ShowBinderFields, Converter={StaticResource BoolToVis}}"/>
                        <TextBox Text="{Binding ViewModel.ActivePage, TargetNullValue='', UpdateSourceTrigger=LostFocus}"
                                 Width="40"
                                 VerticalAlignment="Center"
                                 Margin="0,0,4,0"
                                 Visibility="{Binding ViewModel.ShowBinderFields, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock Text="Slot:"
                                   VerticalAlignment="Center"
                                   Margin="0,0,4,0"
                                   Visibility="{Binding ViewModel.ShowBinderFields, Converter={StaticResource BoolToVis}}"/>
                        <TextBox Text="{Binding ViewModel.ActiveSlot, TargetNullValue='', UpdateSourceTrigger=LostFocus}"
                                 Width="40"
                                 VerticalAlignment="Center"
                                 Margin="0,0,4,0"
                                 Visibility="{Binding ViewModel.ShowBinderFields, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>

                    <!-- Box section field (hidden in audit mode) -->
                    <StackPanel Orientation="Horizontal"
                                Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}">
                        <TextBlock Text="Section:"
                                   VerticalAlignment="Center"
                                   Margin="4,0,4,0"
                                   Visibility="{Binding ViewModel.ShowBoxFields, Converter={StaticResource BoolToVis}}"/>
                        <TextBox Text="{Binding ViewModel.ActiveSection, TargetNullValue='', UpdateSourceTrigger=PropertyChanged}"
                                 Width="100"
                                 VerticalAlignment="Center"
                                 Margin="0,0,4,0"
                                 Visibility="{Binding ViewModel.ShowBoxFields, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>

                    <!-- Other Defaults (hidden in audit mode) -->
                    <Separator Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}"/>
                    <TextBlock VerticalAlignment="Center"
                               Text="Is Foil"
                               Margin="4,0,4,0"
                               Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}"/>
                    <CheckBox IsChecked="{Binding ViewModel.DefaultIsFoil}"
                              VerticalAlignment="Center"
                              Margin="0,0,8,0"
                              Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}"/>
                    <Separator Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}"/>
                    <TextBlock Text="Purchase Price $:"
                               VerticalAlignment="Center"
                               Margin="0,0,4,0"
                               Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}"/>
                    <TextBox Text="{Binding ViewModel.DefaultPurchasePrice, TargetNullValue='', UpdateSourceTrigger=LostFocus}"
                             Width="60"
                             VerticalAlignment="Center"
                             Margin="0,0,8,0"
                             Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}"/>
                </ToolBar>
            </ToolBarTray>
            <ToolBarTray>
                <ToolBar>

                    <TextBlock Text="Scan Quality:"
                               VerticalAlignment="Center"
                               Margin="0,0,4,0"/>
                    <ComboBox SelectedItem="{Binding ViewModel.ScanQuality}"
                              Width="140"
                              VerticalAlignment="Center"
                              Margin="0,0,8,0">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={local:ScanQualityDisplayConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                        <Models:ScanQuality>Fast</Models:ScanQuality>
                        <Models:ScanQuality>HighQuality</Models:ScanQuality>
                    </ComboBox>
                    <Separator/>
                    <Button Command="{Binding ViewModel.ScanCommand}"
                            Content="Scan Cards..."
                            IsEnabled="{Binding ViewModel.IsAuditComplete, Converter={local:InverseBoolConverter}}"/>
                    <Separator Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}"/>
                    <Button Command="{Binding ViewModel.ToggleAuditCompleteCommand}"
                            Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}">
                        <Button.Style>
                            <Style TargetType="Button"
                                   BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Content"
                                        Value="Scans Verified"/>
                                <Setter Property="IsEnabled"
                                        Value="{Binding ViewModel.HasMatchedScans}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ViewModel.IsAuditComplete}"
                                                 Value="True">
                                        <Setter Property="Content"
                                                Value="Undo Verification"/>
                                        <Setter Property="IsEnabled"
                                                Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Separator Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}"/>
                    <Button Command="{Binding ViewModel.CommitScansCommand}"
                            Visibility="{Binding ViewModel.IsAuditMode, Converter={local:InverseBoolToVisibilityConverter}}">
                        <Button.Style>
                            <Style TargetType="Button"
                                   BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Content"
                                        Value="Commit Scans to Collection"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ViewModel.IsCommitting}"
                                                 Value="True">
                                        <Setter Property="Content"
                                                Value="Committing..."/>
                                        <Setter Property="IsEnabled"
                                                Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Separator/>
                    <Button Command="{Binding ViewModel.GenerateAuditReportCommand}"
                            Content="Generate Audit Report"
                            Visibility="{Binding ViewModel.IsAuditMode, Converter={StaticResource BoolToVis}}"/>
                    <Separator/>
                    <Button Command="{Binding ViewModel.ReprocessScansCommand}"
                            Content="Reprocess Unmatched"
                            IsEnabled="{Binding ViewModel.IsAuditComplete, Converter={local:InverseBoolConverter}}"/>
                    <Separator/>
                    <Button Command="{Binding ViewModel.ClearScansCommand}"
                            Content="Clear Queue"
                            IsEnabled="{Binding ViewModel.IsAuditComplete, Converter={local:InverseBoolConverter}}"/>
                </ToolBar>
            </ToolBarTray>
        </ToolBarPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="ScannerListColumn"
                                  Width=".35*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Left panel: filter bar + scanned cards list -->
            <DockPanel>
                <!-- Filter/Sort Bar -->
                <Border DockPanel.Dock="Top"
                        Background="{DynamicResource MaterialDesign.Brush.Card.Background}"
                        Padding="6,4"
                        BorderBrush="{DynamicResource MaterialDesign.Brush.TextBox.HoverBackground}"
                        BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Sort -->
                        <StackPanel Grid.Column="0"
                                    Orientation="Horizontal">
                            <TextBlock Text="Sort:"
                                       VerticalAlignment="Center"
                                       Margin="0,0,4,0"/>
                            <ComboBox SelectedItem="{Binding ViewModel.ScanSortField}"
                                      Width="110"
                                      VerticalAlignment="Center"
                                      Margin="0,0,4,0">
                                <Models:ScanSortField>None</Models:ScanSortField>
                                <Models:ScanSortField>Confidence</Models:ScanSortField>
                                <Models:ScanSortField>Name</Models:ScanSortField>
                                <Models:ScanSortField>Set</Models:ScanSortField>
                            </ComboBox>
                            <ToggleButton IsChecked="{Binding ViewModel.ScanSortAscending}"
                                          Padding="6,2"
                                          ToolTip="Toggle ascending/descending"
                                          Style="{StaticResource MaterialDesignFlatPrimaryToggleButton}">
                                <ToggleButton.Content>
                                    <TextBlock>
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text"
                                                        Value="&#x2191;"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}"
                                                                 Value="False">
                                                        <Setter Property="Text"
                                                                Value="&#x2193;"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </ToggleButton.Content>
                            </ToggleButton>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Scan statistics bar (clickable filters) -->
                <Border DockPanel.Dock="Bottom"
                        Background="{DynamicResource MaterialDesign.Brush.Card.Background}"
                        Padding="4,2"
                        BorderBrush="{DynamicResource MaterialDesign.Brush.TextBox.HoverBackground}"
                        BorderThickness="0,1,0,0">
                    <StackPanel Orientation="Horizontal">
                        <!-- Scanned (clear filters) -->
                        <Button Command="{Binding ViewModel.SetScanFilterCommand}"
                                Padding="6,2" Margin="0,0,2,0"
                                Style="{StaticResource MaterialDesignFlatButton}">
                            <Button.CommandParameter>
                                <Models:ScanFilterMode>None</Models:ScanFilterMode>
                            </Button.CommandParameter>
                            <TextBlock FontSize="11">
                                <Run Text="Scanned:"/>
                                <Run Text="{Binding ViewModel.ScanCount, Mode=OneWay}" FontWeight="SemiBold"/>
                            </TextBlock>
                        </Button>

                        <TextBlock Text="|" VerticalAlignment="Center" Margin="2,0"
                                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}" FontSize="11"/>

                        <!-- Avg Confidence (display only) -->
                        <TextBlock FontSize="11" VerticalAlignment="Center" Padding="6,2" Margin="0,0,2,0"
                                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}">
                            <Run Text="Avg Confidence:"/>
                            <Run Text="{Binding ViewModel.ScanAvgConfidence, StringFormat='{}{0:F0}%', Mode=OneWay}"
                                 FontWeight="SemiBold"/>
                        </TextBlock>

                        <TextBlock Text="|" VerticalAlignment="Center" Margin="2,0"
                                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}" FontSize="11"/>

                        <!-- High (filter) -->
                        <Button Command="{Binding ViewModel.SetScanFilterCommand}"
                                Padding="6,2" Margin="0,0,2,0"
                                Style="{StaticResource MaterialDesignFlatButton}">
                            <Button.CommandParameter>
                                <Models:ScanFilterMode>HighConfidence</Models:ScanFilterMode>
                            </Button.CommandParameter>
                            <TextBlock FontSize="11">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ViewModel.ScanFilterMode}"
                                                         Value="{x:Static Models:ScanFilterMode.HighConfidence}">
                                                <Setter Property="Foreground"
                                                        Value="{DynamicResource MaterialDesign.Brush.Primary}"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <Run Text="High:"/>
                                <Run Text="{Binding ViewModel.ScanHighConfidenceCount, Mode=OneWay}" FontWeight="SemiBold"/>
                            </TextBlock>
                        </Button>

                        <TextBlock Text="|" VerticalAlignment="Center" Margin="2,0"
                                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}" FontSize="11"/>

                        <!-- Low (filter) -->
                        <Button Command="{Binding ViewModel.SetScanFilterCommand}"
                                Padding="6,2" Margin="0,0,2,0"
                                Style="{StaticResource MaterialDesignFlatButton}">
                            <Button.CommandParameter>
                                <Models:ScanFilterMode>LowConfidence</Models:ScanFilterMode>
                            </Button.CommandParameter>
                            <TextBlock FontSize="11">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ViewModel.ScanFilterMode}"
                                                         Value="{x:Static Models:ScanFilterMode.LowConfidence}">
                                                <Setter Property="Foreground"
                                                        Value="{DynamicResource MaterialDesign.Brush.Primary}"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <Run Text="Low:"/>
                                <Run Text="{Binding ViewModel.ScanLowConfidenceCount, Mode=OneWay}" FontWeight="SemiBold"/>
                            </TextBlock>
                        </Button>

                        <TextBlock Text="|" VerticalAlignment="Center" Margin="2,0"
                                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}" FontSize="11"/>

                        <!-- Flagged (filter) -->
                        <Button Command="{Binding ViewModel.SetScanFilterCommand}"
                                Padding="6,2"
                                Style="{StaticResource MaterialDesignFlatButton}">
                            <Button.CommandParameter>
                                <Models:ScanFilterMode>Flagged</Models:ScanFilterMode>
                            </Button.CommandParameter>
                            <TextBlock FontSize="11">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ViewModel.ScanFilterMode}"
                                                         Value="{x:Static Models:ScanFilterMode.Flagged}">
                                                <Setter Property="Foreground"
                                                        Value="{DynamicResource MaterialDesign.Brush.Primary}"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <Run Text="Flagged:"/>
                                <Run Text="{Binding ViewModel.ScanFlaggedCount, Mode=OneWay}" FontWeight="SemiBold"/>
                            </TextBlock>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Scanned cards list -->
                <ListView x:Name="ScannedCardsListView"
                          ItemsSource="{Binding ViewModel.ScanView}"
                          SelectionMode="Extended"
                          SelectionChanged="ScannedCardsListView_SelectionChanged"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling"
                          ScrollViewer.IsDeferredScrollingEnabled="True">
                    <ListView.ContextMenu>
                        <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                            <MenuItem Header="Open Card Detail"
                                      Command="{Binding ViewModel.CardDoubleClickCommand}"/>
                            <MenuItem Header="Copy Card Name(s)"
                                      Command="{Binding ViewModel.CopyScannedCardNamesCommand}"/>
                            <Separator/>
                            <MenuItem Header="Set Condition">
                                <MenuItem Header="NM"
                                          Command="{Binding ViewModel.SetScannedConditionCommand}"
                                          CommandParameter="NM"/>
                                <MenuItem Header="LP"
                                          Command="{Binding ViewModel.SetScannedConditionCommand}"
                                          CommandParameter="LP"/>
                                <MenuItem Header="MP"
                                          Command="{Binding ViewModel.SetScannedConditionCommand}"
                                          CommandParameter="MP"/>
                                <MenuItem Header="HP"
                                          Command="{Binding ViewModel.SetScannedConditionCommand}"
                                          CommandParameter="HP"/>
                                <MenuItem Header="D"
                                          Command="{Binding ViewModel.SetScannedConditionCommand}"
                                          CommandParameter="D"/>
                            </MenuItem>
                            <MenuItem Header="Set Foil"
                                      Command="{Binding ViewModel.SetScannedFoilCommand}"
                                      CommandParameter="True"/>
                            <MenuItem Header="Set Non-Foil"
                                      Command="{Binding ViewModel.SetScannedFoilCommand}"
                                      CommandParameter="False"/>
                            <Separator/>
                            <MenuItem Header="Assign Match"
                                      Command="{Binding ViewModel.AssignMatchCommand}"/>
                            <Separator/>
                            <MenuItem Header="Remove from Queue"
                                      Command="{Binding ViewModel.RemoveScannedCardCommand}"/>
                        </ContextMenu>
                    </ListView.ContextMenu>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem"
                               BasedOn="{StaticResource {x:Type ListViewItem}}">
                            <Setter Property="HorizontalContentAlignment"
                                    Value="Stretch"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsFlagged}"
                                        Value="True">
                                    <Setter Property="BorderBrush"
                                            Value="Orange"/>
                                    <Setter Property="BorderThickness"
                                            Value="3,0,0,0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Scanned image (top left) -->
                                <Image Grid.Row="0" Grid.Column="0"
                                       Source="{Binding TempImagePath, Converter={local:ScanImageConverter}}"
                                       Width="{Binding DataContext.ViewModel.CardPreviewWidth,
                                           RelativeSource={RelativeSource AncestorType=ListView}}"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Stretch="Uniform"
                                       Margin="0,0,4,0"/>

                                <!-- Matched card art (top right) -->
                                <Image Grid.Row="0" Grid.Column="1"
                                       Width="{Binding DataContext.ViewModel.CardPreviewWidth,
                                           RelativeSource={RelativeSource AncestorType=ListView}}"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Stretch="Uniform"
                                       Margin="4,0,0,0">
                                    <Image.Source>
                                        <MultiBinding Converter="{local:MatchedArtConverter}">
                                            <Binding Path="Match.LocalImagePath"/>
                                            <Binding Path="Match.ImageUri"/>
                                        </MultiBinding>
                                    </Image.Source>
                                </Image>

                                <!-- Card info below both images -->
                                <StackPanel Grid.Row="1" Grid.ColumnSpan="2"
                                            Margin="0,4,0,0">
                                    <!-- Card Name -->
                                    <TextBlock Text="{Binding Match.Name}"
                                               FontWeight="Bold"
                                               FontSize="{Binding DataContext.ViewModel.ScannerFontSize,
                                                   RelativeSource={RelativeSource AncestorType=ListView}}"/>

                                    <!-- Set Name (Set Code) [symbol] -->
                                    <StackPanel Orientation="Horizontal"
                                                Margin="0,2,0,0">
                                        <TextBlock Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"
                                                   FontSize="{Binding DataContext.ViewModel.ScannerFontSizeSmall,
                                                       RelativeSource={RelativeSource AncestorType=ListView}}"
                                                   VerticalAlignment="Center">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0} ({1})">
                                                    <Binding Path="Match.SetName"/>
                                                    <Binding Path="Match.SetCode"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                        <Border Width="{Binding DataContext.ViewModel.ScannerFontSizeSmall,
                                                    RelativeSource={RelativeSource AncestorType=ListView}}"
                                                Height="{Binding DataContext.ViewModel.ScannerFontSizeSmall,
                                                    RelativeSource={RelativeSource AncestorType=ListView}}"
                                                Margin="6,0,0,0"
                                                VerticalAlignment="Center"
                                                helpers:SetSymbol.SetCode="{Binding Match.SetCode}"
                                                helpers:SetSymbol.Rarity="{Binding Match.Rarity}"/>
                                    </StackPanel>

                                    <!-- #: Collector Number -->
                                    <TextBlock Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"
                                               FontSize="{Binding DataContext.ViewModel.ScannerFontSizeSmall,
                                                   RelativeSource={RelativeSource AncestorType=ListView}}"
                                               Margin="0,2,0,0">
                                        <Run Text="#:"/>
                                        <Run Text="{Binding Match.CollectorNumber, Mode=OneWay}"/>
                                    </TextBlock>

                                    <!-- Match Confidence -->
                                    <TextBlock Margin="0,2,0,0"
                                               FontSize="{Binding DataContext.ViewModel.ScannerFontSizeSmall,
                                                   RelativeSource={RelativeSource AncestorType=ListView}}"
                                               Visibility="{Binding Match.Confidence, Converter={local:NullToCollapsedConverter}}">
                                        <Run Text="Match Confidence:"
                                             Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"/>
                                        <Run Text="{Binding Match.Confidence, StringFormat='{}{0:F0}%', Mode=OneWay}"
                                             Foreground="{Binding Match.Confidence, Converter={local:ConfidenceToColorConverter}}"/>
                                    </TextBlock>

                                    <!-- Match and Flag buttons side by side -->
                                    <StackPanel Orientation="Horizontal"
                                                Margin="0,4,0,0">
                                        <!-- Match button (low confidence only) -->
                                        <Button Content="Match"
                                                Padding="8,2"
                                                HorizontalAlignment="Left"
                                                Command="{Binding DataContext.ViewModel.ConfirmMatchCommand,
                                                    RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding}"
                                                Visibility="{Binding Match.Confidence, Converter={local:LowConfidenceToVisibleConverter}}"
                                                Style="{StaticResource MaterialDesignFlatButton}"/>

                                        <!-- Flag button -->
                                        <Button Padding="8,2"
                                                Margin="4,0,0,0"
                                                HorizontalAlignment="Left"
                                                Command="{Binding DataContext.ViewModel.ToggleFlagCommand,
                                                    RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource MaterialDesignFlatButton}">
                                            <Button.Content>
                                                <TextBlock>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text"
                                                                    Value="Flag"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsFlagged}"
                                                                        Value="True">
                                                                    <Setter Property="Text"
                                                                            Value="Unflag"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Button.Content>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.InputBindings>
                        <KeyBinding Key="Delete"
                                    Command="{Binding ViewModel.RemoveScannedCardCommand}"/>
                    </ListView.InputBindings>
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseDoubleClick">
                            <i:InvokeCommandAction Command="{Binding ViewModel.CardDoubleClickCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ListView>
            </DockPanel>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1"
                          Width="5"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Stretch"
                          Background="{DynamicResource MaterialDesign.Brush.TextBox.HoverBackground}"
                          DragCompleted="GridSplitter_DragCompleted"/>

            <!-- Detail / Edit Panel -->
            <local:ScannerDetailPanelView x:Name="DetailPanel"
                                          Grid.Column="2"/>
        </Grid>
    </Grid>
</UserControl>
