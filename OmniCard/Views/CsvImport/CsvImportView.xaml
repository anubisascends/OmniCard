<Window x:Class="OmniCard.Views.CsvImport.CsvImportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OmniCard.Views.CsvImport"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Import Collection" Height="500" Width="650"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False" ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance {x:Type local:CsvImportView}}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{StaticResource AppFont}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Format + counts -->
        <StackPanel Margin="0,0,0,8">
            <TextBlock Text="{Binding ViewModel.FormatLabel}" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,4"/>
            <TextBlock Text="{Binding ViewModel.CardCountLabel}" Margin="0,0,0,2"/>
            <TextBlock Text="{Binding ViewModel.WarningLabel}" Foreground="OrangeRed"
                       Visibility="{Binding ViewModel.HasWarnings, Converter={StaticResource BoolToVis}}"/>
        </StackPanel>

        <!-- Duplicate handling -->
        <GroupBox Grid.Row="1" Header="Duplicate Handling" Margin="0,0,0,8" Padding="4">
            <StackPanel>
                <RadioButton Content="Skip duplicates (same card, condition, and foil)"
                             IsChecked="{Binding ViewModel.SkipDuplicates}" Margin="0,2"/>
                <RadioButton Content="Add all as new cards" Margin="0,2"/>
            </StackPanel>
        </GroupBox>

        <!-- Location assignment -->
        <GroupBox Grid.Row="2" Header="Assign to Location" Margin="0,0,0,8" Padding="4">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Location:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox ItemsSource="{Binding ViewModel.AvailableContainers}"
                          SelectedItem="{Binding ViewModel.SelectedContainer}"
                          DisplayMemberPath="Name"
                          Width="250"
                          VerticalAlignment="Center"/>
            </StackPanel>
        </GroupBox>

        <!-- Preview label -->
        <TextBlock Grid.Row="3" Text="Preview (first 20 rows):" FontWeight="SemiBold" Margin="0,0,0,4"/>

        <!-- Preview grid -->
        <DataGrid Grid.Row="4" ItemsSource="{Binding ViewModel.PreviewCards}"
                  AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Set" Binding="{Binding SetName}" Width="120"/>
                <DataGridTextColumn Header="#" Binding="{Binding Number}" Width="60"/>
                <DataGridTextColumn Header="Condition" Binding="{Binding Condition}" Width="70"/>
                <DataGridCheckBoxColumn Header="Foil" Binding="{Binding IsFoil}" Width="40"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Import" Command="{Binding ViewModel.ImportCommand}"
                    IsEnabled="{Binding ViewModel.CanImport}"
                    Padding="16,4" Margin="0,0,8,0"/>
            <Button Content="Cancel" Command="{Binding ViewModel.CancelCommand}"
                    Padding="16,4" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
