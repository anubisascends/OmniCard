<Window x:Class="OmniCard.Views.SealedProductEditor.SealedProductTemplateEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OmniCard.Views.SealedProductEditor"
        xmlns:models="clr-namespace:OmniCard.Models"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Title="Sealed Product Template" Height="550" Width="600"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{StaticResource AppFont}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Product details -->
        <Border Background="{DynamicResource MaterialDesign.Brush.Card.Background}"
                CornerRadius="4" Padding="8" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/><RowDefinition/><RowDefinition/>
                </Grid.RowDefinitions>

                <TextBlock Text="Name:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,4,8,4"/>
                <TextBox Grid.Column="1" Grid.ColumnSpan="3"
                         Text="{Binding ViewModel.Name, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center" Margin="0,2"/>

                <TextBlock Grid.Row="1" Text="Type:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,4,8,4"/>
                <ComboBox Grid.Row="1" Grid.Column="1"
                          SelectedItem="{Binding ViewModel.ProductType}" Margin="0,2,8,2">
                    <models:SealedProductType>Case</models:SealedProductType>
                    <models:SealedProductType>BoosterBox</models:SealedProductType>
                    <models:SealedProductType>BundleBox</models:SealedProductType>
                    <models:SealedProductType>CollectorBoosterPack</models:SealedProductType>
                    <models:SealedProductType>BoosterPack</models:SealedProductType>
                    <models:SealedProductType>PromoPack</models:SealedProductType>
                    <models:SealedProductType>FixedPack</models:SealedProductType>
                    <models:SealedProductType>Card</models:SealedProductType>
                </ComboBox>

                <TextBlock Grid.Row="1" Grid.Column="2" Text="Set:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="8,4,8,4"/>
                <TextBox Grid.Row="1" Grid.Column="3"
                         Text="{Binding ViewModel.SetCode, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center" Margin="0,2"/>

                <TextBlock Grid.Row="2" Text="UPC:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,4,8,4"/>
                <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3"
                         Text="{Binding ViewModel.Upc, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center" Margin="0,2"/>
            </Grid>
        </Border>

        <!-- Contents header + add button -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
            <TextBlock Text="Contents (what this product contains when cracked):"
                       FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Button Content="Add" Padding="12,2"
                    Command="{Binding ViewModel.AddContentLineCommand}"/>
        </StackPanel>

        <!-- Contents list -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,0,0,8">
            <ItemsControl ItemsSource="{Binding ViewModel.ContentLines}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:ContentLineItem}">
                        <Border BorderBrush="{DynamicResource MaterialDesign.Brush.DataGrid.ColumnHeader.Border}"
                                BorderThickness="0,0,0,1" Padding="4" Margin="0,0,0,4">
                            <StackPanel Orientation="Horizontal">
                                <TextBox Text="{Binding Quantity, UpdateSourceTrigger=LostFocus}"
                                         Width="50" Margin="0,0,8,0" VerticalContentAlignment="Center"/>
                                <TextBlock Text="x" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <ComboBox SelectedItem="{Binding ChildProductType}" Width="160" Margin="0,0,8,0">
                                    <models:SealedProductType>Case</models:SealedProductType>
                                    <models:SealedProductType>BoosterBox</models:SealedProductType>
                                    <models:SealedProductType>BundleBox</models:SealedProductType>
                                    <models:SealedProductType>CollectorBoosterPack</models:SealedProductType>
                                    <models:SealedProductType>BoosterPack</models:SealedProductType>
                                    <models:SealedProductType>PromoPack</models:SealedProductType>
                                    <models:SealedProductType>FixedPack</models:SealedProductType>
                                    <models:SealedProductType>Card</models:SealedProductType>
                                </ComboBox>
                                <TextBlock Text="{Binding ChildTemplateName, TargetNullValue='(pick at crack time)'}"
                                           VerticalAlignment="Center" FontStyle="Italic"
                                           Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"
                                           Margin="0,0,8,0"/>
                                <Button Content="X" Padding="6,2"
                                        Command="{Binding DataContext.ViewModel.RemoveContentLineCommand,
                                            RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding ViewModel.CancelCommand}"
                    IsCancel="True" Padding="16,6" Margin="0,0,8,0"/>
            <Button Content="Save" Command="{Binding ViewModel.SaveCommand}"
                    IsDefault="True" Padding="16,6" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
