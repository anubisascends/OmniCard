<Window x:Class="OmniCard.Views.SealedProductEditor.CrackProductView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OmniCard.Views.SealedProductEditor"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Title="Crack Open Product" Height="400" Width="550"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{StaticResource AppFont}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Product info -->
        <StackPanel Margin="0,0,0,12">
            <TextBlock Text="{Binding ViewModel.ProductName}" FontWeight="Bold" FontSize="16"/>
            <TextBlock Text="{Binding ViewModel.ProductInfo}"
                       Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"/>
        </StackPanel>

        <!-- Content lines -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,8">
            <ItemsControl ItemsSource="{Binding ViewModel.ContentLines}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:CrackContentLine}">
                        <Border Padding="8" Margin="0,0,0,4"
                                Background="{DynamicResource MaterialDesign.Brush.Card.Background}"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock VerticalAlignment="Center" Margin="0,0,12,0">
                                    <Run Text="{Binding Quantity, Mode=OneWay}"/>
                                    <Run Text="x"/>
                                    <Run Text="{Binding ChildProductType, Mode=OneWay}"/>
                                </TextBlock>

                                <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                           Text="{Binding ChildTemplateName, TargetNullValue='(not assigned)'}"
                                           FontStyle="Italic"
                                           Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"/>

                                <Button Grid.Column="2" Content="Pick Template..."
                                        Padding="8,2"
                                        Visibility="{Binding NeedsTemplate, Converter={StaticResource BoolToVis}}"
                                        Command="{Binding DataContext.ViewModel.PickTemplateCommand,
                                            RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding ViewModel.CancelCommand}"
                    IsCancel="True" Padding="16,6" Margin="0,0,8,0"/>
            <Button Content="Crack Open" Command="{Binding ViewModel.CrackCommand}"
                    IsDefault="True" Padding="16,6" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
