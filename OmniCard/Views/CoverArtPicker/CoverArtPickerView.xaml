<Window x:Class="OmniCard.Views.CoverArtPicker.CoverArtPickerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OmniCard.Views.CoverArtPicker"
        xmlns:conv="clr-namespace:OmniCard.Controls.Converters;assembly=OmniCard.Controls"
        xmlns:helpers="clr-namespace:OmniCard.Controls;assembly=OmniCard.Controls"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Title="{Binding ViewModel.ContainerName, StringFormat='Choose Cover Art for {0}'}"
        Height="500" Width="700"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        TextElement.FontSize="13"
        FontFamily="{StaticResource AppFont}">

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filter -->
        <TextBox Text="{Binding ViewModel.FilterText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,12,8"
                 VerticalContentAlignment="Center"
                 Tag="Filter by card name..."/>

        <!-- Card list -->
        <ListView Grid.Row="1"
                  ItemsSource="{Binding ViewModel.FilteredCards}"
                  SelectedItem="{Binding ViewModel.SelectedCard}"
                  Margin="0,0,12,8">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <Image Source="{Binding ImageUri}" Width="40" Height="56"
                               Stretch="Uniform" Margin="0,0,8,0"
                               Visibility="{Binding ImageUri, Converter={conv:NullToCollapsedConverter}}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal">
                                <Border Width="14" Height="14" Margin="0,0,4,0"
                                        helpers:SetSymbol.SetCode="{Binding SetCode}"
                                        helpers:SetSymbol.Rarity="{Binding Rarity}"/>
                                <TextBlock Text="{Binding SetName}"
                                           Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"/>
                                <TextBlock Text="{Binding Number, StringFormat=' #{0}'}"
                                           Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Light}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Preview panel -->
        <Border Grid.Column="1" Grid.RowSpan="2"
                Background="{DynamicResource MaterialDesign.Brush.Card.Background}"
                CornerRadius="4" Padding="8" Width="220">
            <Image Source="{Binding ViewModel.PreviewImageUri}"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality"/>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding ViewModel.CancelCommand}"
                    Padding="16,6" Margin="0,0,8,0"/>
            <Button Content="OK" Command="{Binding ViewModel.ConfirmCommand}"
                    Padding="16,6" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
