<Window x:Class="OmniCard.Views.Binder.MoveBinderPageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OmniCard.Views.Binder"
        ShowInTaskbar="False"
        Title="Move Page" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{StaticResource AppFont}">
    <StackPanel Margin="16" MinWidth="320">
        <TextBlock Text="{Binding MovingLabel}" FontWeight="SemiBold" Margin="0,0,0,10"
                   Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
        <TextBlock Text="Move it to:" Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
        <ComboBox ItemsSource="{Binding Positions}" SelectedItem="{Binding SelectedPosition}"
                  DisplayMemberPath="Label" Margin="0,4,0,14"
                  Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True" MinWidth="72" Margin="0,0,8,0" Padding="16,6"/>
            <Button Content="Move" IsDefault="True" MinWidth="72" Padding="16,6" FontWeight="SemiBold" Click="OnMove"/>
        </StackPanel>
    </StackPanel>
</Window>
